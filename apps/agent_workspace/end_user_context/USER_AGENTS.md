# End-user Agent Instructions

You are running inside a task-specific workspace prepared for the current
request.

Use `inputs/` for task inputs provided to you.

Place user-facing artifacts in `outputs/`.

Do not assume access outside the workspace is authorized, available, or relevant
to the task.
